import { MenuMarkupOptions } from "./types";

export const defaultMenuKeyboardOptions: MenuMarkupOptions = {
  persistent: false,
  selective: false,
  placeholder: "",
  oneTime: false,
  resized: false,
  columns: 2
};
