import { Location, LocationsList } from "./types";

// FIXME:TODO: implement
/**
 * This method gets data from the server
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const getTabData = (_tab: LocationsList): Location => {
  return {label: "some label", content: "some content"};
};